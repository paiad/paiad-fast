spring:
  #mysql
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 这里需要切换为自己的数据库地址
    url: jdbc:mysql://localhost:3306/hash?useSSL=false&serverTimezone=GMT%2b8&rewriteBatchedStatements=true
    username: root
    password: root

  #redis
  data:
    redis:
      host: localhost
      port: 6379
      password: 1daw3o4fe5z6s
      database: 0

